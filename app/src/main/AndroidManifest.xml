<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tubes_mobile">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tubes_mobile">
        <activity
            android:name="com.example.tubes_mobile.activities.OpenNewsActivity"
            android:exported="false" />
        <activity
            android:name="com.example.tubes_mobile.activities.HeadLineActivity"
            android:exported="false"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".RegisterActivity"
            android:exported="true"
            android:label="Register"
            android:usesCleartextTraffic="true"
            tools:ignore="AllowBackup,GoogleAppIndexingWarning,UnusedAttribute" />
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.tubes_mobile.activities.MainActivity"
            android:exported="true" />
    </application>

</manifest>